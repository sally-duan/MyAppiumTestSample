xdescribe("firstTest", () => {
  xit("should pause for 100 seconds", async () => {
    console.log(browser.pause); // This will log the pause function if available

    await browser.pause(1000); // Pause for 100 seconds (100,000 ms)
    console.log("sally succeed");
  });
});
